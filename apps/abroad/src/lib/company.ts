/**
 * 사업자 정보 — 단일 출처.
 *
 * 전자상거래법상 홈페이지 하단에 노출해야 하는 항목이자,
 * 카드사 심사용 "결제경로 파일"의 하단정보 캡처(토스 가이드 7쪽) 필수 구성이다.
 * 값이 바뀌면 여기만 고치면 푸터·약관·환불규정에 함께 반영된다.
 *
 * 출처: 사업자등록증(2025-08-07 발급, 성동세무서).
 */

export const COMPANY = {
  /**
   * 상호명(법인명) — 사업자등록증 표기와 **글자 그대로** 같아야 한다.
   * 영문 병기까지 포함해서 한 덩어리다. 토스 심사 보완 요청(2026-08)으로
   * "주식회사 글로케어" → "주식회사 글로케어 (Glocare Corp.)" 로 맞췄다.
   * 임의로 줄이지 말 것 — 푸터·약관·환불규정·주문서 판매자 표기가 전부 이 값이다.
   */
  name: "주식회사 글로케어 (Glocare Corp.)",
  /** 대표자명 */
  ceo: "홍강식",
  /** 사업자등록번호 */
  businessNo: "239-87-03310",
  /**
   * 통신판매업신고번호 — 아직 없다. 순서가 반대라서 그렇다:
   * PG 심사 통과 → 구매안전서비스 이용확인증 발급 → 구청 신고 → 번호.
   * 그래서 카드사 심사 시점엔 비어 있는 게 정상이고, 푸터엔 "준비 중"으로 나간다
   * (토스 담당자 확인, 2026-08). 번호가 나오면 여기만 채우면 푸터·약관·환불규정에
   * 함께 반영된다.
   */
  mailOrderNo: "",
  /** 사업장 주소 */
  address:
    "서울특별시 광진구 능동로 120, 2층 202호 창의관(화양동, 건국대학교)",
  /** 고객센터 유선전화 */
  tel: "02-456-0724",
  email: "help@glocare.co.kr",
  /** 베트남 현지 연락 채널 */
  zalo: "+82-10-2256-8724",
  mobile: "0977.456.324",
  /** 개인정보 보호책임자 */
  privacyOfficer: "홍강식",
} as const;

/** 미기재 항목은 빈 문자열 대신 안내 문구로 — 심사에서 누락으로 보이지 않게. */
export function companyField(v: string, fallback = "준비 중"): string {
  return v.trim() ? v : fallback;
}
