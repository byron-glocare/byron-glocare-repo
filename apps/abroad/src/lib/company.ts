/**
 * 사업자 정보 — 단일 출처.
 *
 * 전자상거래법상 홈페이지 하단에 노출해야 하는 항목이자,
 * 카드사 심사용 "결제경로 파일"의 하단정보 캡처(토스 가이드 7쪽) 필수 구성이다.
 * 값이 바뀌면 여기만 고치면 푸터·약관·환불규정에 함께 반영된다.
 *
 * 출처: 사업자등록증(2025-08-07 발급, 성동세무서).
 */

export const COMPANY = {
  /** 상호명(법인명) */
  name: "주식회사 글로케어",
  nameEn: "Glocare Corp.",
  /** 대표자명 */
  ceo: "홍강식",
  /** 사업자등록번호 */
  businessNo: "239-87-03310",
  /**
   * 통신판매업신고번호 — 구청 신고 진행 중(2026-08). 번호 나오면 여기만 채우면
   * 푸터·약관·환불규정에 함께 반영된다. 카드사 심사 필수 항목이므로 빈 채로 제출 불가.
   */
  mailOrderNo: "",
  /** 사업장 주소 */
  address:
    "서울특별시 광진구 능동로 120, 2층 202호 창의관(화양동, 건국대학교)",
  /** 고객센터 유선전화 */
  tel: "02-456-0724",
  email: "help@glocare.co.kr",
  /** 베트남 현지 연락 채널 */
  zalo: "+82-10-2256-8724",
  mobile: "0977.456.324",
  /** 개인정보 보호책임자 */
  privacyOfficer: "홍강식",
} as const;

/** 미기재 항목은 빈 문자열 대신 안내 문구로 — 심사에서 누락으로 보이지 않게. */
export function companyField(v: string, fallback = "준비 중"): string {
  return v.trim() ? v : fallback;
}
